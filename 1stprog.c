
#include <stdio.h>

int main()
{
    printf("Hello! My Name Is Satyaki,1st Year BTech CSE Student At HIT.\n");
    return 0;
}
// KEY SHORTCUTS...

/* CTRL + L : Clear the terminal screen
   CTRL + D : Exit the terminal
   CTRL + C : Stop the running program
   CTRL + ALT + N : Run the program
   CTRL + , : Open the settings in VS Code
   CTRL + S : Save the file
   SHIFT + DEL : Delete the line
   CTRL + H : Find and replace the text
   CTRL + F : Find the text
   CTRL + Z : Undo the changes
   CTRL + Y : Redo the changes
   SHIFT + ALT + F: used in formatting the code (presenting the code in a proper way)
   TAB → indent/move selected lines right
   SHIFT + TAB → unindent/move selected lines left

   IMPORTANT...
    %f       → 6 decimal places
    %.0f     → 0 decimal places
    %.1f     → 1 decimal place
    %.2f     → 2 decimal places
    %.3f     → 3 decimal places
    %.4f     → 4 decimal places
    %.5f     → 5 decimal 
    
    ARITMETIC OPERATIONS PRECEDENCE IN C...

    *, /, % → Multiplication, Division, Modulus > +, -    → Addition, Subtraction

    FOR *, /, % :  Left to Right that is, if there are multiple *, /, % operators in a single expression,
    then they will be evaluated from left to right.

    FOR +, - : Left to Right...
    

    
    

    ...
  
*/