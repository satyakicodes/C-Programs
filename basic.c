#include <stdio.h>  
int main()
{
    int SG, AM;
    printf("Enter the age of SG: ");
    scanf("%d", &SG);
    printf("Enter the age of AM: ");
    scanf("%d", &AM);
    printf("Together we become : %d", SG+AM);
}
